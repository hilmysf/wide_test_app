const Increment = ({ onIncrease }) => {
    return <button style={{ padding: "8px", borderRadius: "16px", borderWidth: "0.5px" }} onClick={onIncrease}>
        +
    </button>
}

export default Increment;